# 📘 دليل NEXA Factory الشامل

نظام مكوّن من **3 طبقات منفصلة تماماً**، كل وحدة عندها صلاحيات وواجهة مختلفة:

```
1️⃣ بوت المصنع (factory_bot.py)          ← أي شخص يصنع بوت منه
2️⃣ لوحة تحكم المصنع (factory_admin/)     ← أنت بس (أو مشرف تضيفه)
3️⃣ لوحة تحكم كل بوت مصنوع (admin_panel/) ← صاحب ذاك البوت بالذات
   + تجربة المستخدم العادي (بس رسالة ترحيب)
```

كل الأزرار بكل مكان **Inline** (ملصقة تحت الرسالة نفسها) — صفر أزرار تحت الشاشة، إلا استثناء واحد وحيد مذكور بالقسم 6.

---

## 1️⃣ بوت المصنع — تجربة أي شخص عادي

### `/start`
```
بوت صنع سايت الافضل طاها
• يمكنك صنع بوتك الان مدفوع ، مجانا .
• قم بصنع بوتك الان بالعديد من المميزات .
```
**الأزرار:**

| الزر | شنو يسوي | شنو يطلع بعده |
|---|---|---|
| 🤖 صنع بوت جديد | يبدأ محادثة "أرسل التوكن" | 3 أزرار مساعدة + ينتظر رسالتك النصية |
| 📋 قائمة بوتاتك | يجيب كل بوتاتك من قاعدة البيانات | صف لكل بوت: زر يفتحه مباشرة (↗) + زر "معلومات اكثر" |
| ❓ كيف اصنع بوت؟ | نص شرح 3 خطوات ثابت | زر "صنع بوت جديد" + رجوع |
| ⭐ النسخة المدفوعة | — | نص "قيد التطوير" فقط |

### مسار "صنع بوت جديد" بالتفصيل
1. تضغط الزر → يجيك: *"🎯 سايت — 🔑 ارسل التوكن الخاص بك الان"*
2. ترسل التوكن كنص عادي (مثال: `123456:ABCDefg...`)
3. **يحصل فعلياً بالخلفية** (كل خطوة تنفّذ حقيقي مو محاكاة):
   - يتحقق من صيغة التوكن (فيه `:` والجزء الأول رقم)
   - يتأكد ما أحد سجله قبل
   - **يفحص هل أنت محظور من إنشاء البوتات** (من لوحة المصنع)
   - **يفحص هل وصلت الحد الأقصى** لعدد البوتات المسموح لك (لو الأدمن حدده)
   - يتصل فعلياً بتيليجرام (`getMe`) للتأكد التوكن شغال
   - يسجل البوت بقاعدة البيانات
   - **يشغّله polling حي فوراً** (يبدأ يستقبل رسائل بنفس اللحظة، بدون إعادة تشغيل أي شي)
4. يجيك تأكيد + زر يفتح بوتك الجديد مباشرة

### زر "معلومات اكثر" (بقائمة بوتاتك)
يعرض: اسم البوت، تاريخ الإنشاء، عدد مستخدميه + زر "🗑 حذف البوت" (يوقف الـ polling ويمسحه نهائياً من القاعدة).

### 🔐 الاشتراك الإجباري
لو أنت (أدمن المصنع) ضفت قناة إجبارية من لوحة المصنع، **أي شخص** (حتى قبل ما يوصل لقائمة "صنع بوت جديد") لازم يشترك فيها أول، وإلا يوصله رسالة تطلب الاشتراك مع زر "✅ تحقق".

---

## 2️⃣ لوحة تحكم المصنع — لك أنت بس

يدخلها **بس** صاحب بوت المصنع الأصلي، أو أي شخص تضيفه كمشرف من قسم "👑 المشرفون". أي شخص ثاني يكتب `/start` لبوت المصنع **يشوف الواجهة العادية بالأعلى فقط** — ما عنده أي طريقة يشوف لوحة المصنع حتى لو حاول.

### الشاشة الرئيسية
```
🏭 NEXA FACTORY
🟢 Engine Online
🤖 127 بوت | 👤 83 مالك | 👥 54,281 مستخدم
📨 32,845 رسالة اليوم | 📢 184 إذاعة اليوم | 🚫 1,284 حظر
```
كل رقم هذا **حقيقي** — مسحوب مباشرة من قاعدة البيانات وقت ما تفتح الشاشة، مو ثابت.

**شبكة الأزرار (12 قسم):**

### 🤖 البوتات
| الزر | شنو يسوي |
|---|---|
| 🔍 بحث عن بوت | يفتح محادثة، ترسل اسم أو ID، يطلعلك النتائج |
| 📋 جميع البوتات | قائمة مقسّمة صفحات (8 بكل صفحة)، بأزرار "◀ السابق / التالي ▶" |
| 🆕 آخر البوتات | نفس القائمة مرتبة بالأحدث أول |

تضغط على أي بوت من القائمة → يطلعلك:
```
🤖 معلومات البوت
الاسم: @example_bot
ID: 123456789
🔑 التوكن: 123456789:ABCDefg...   ← كامل وواضح (بما إنك الأدمن)
👤 المالك ID: 987654321
📅 الإنشاء: 2026-08-16
👥 المستخدمون: 1,245
📨 الرسائل: 8,421
📢 الإذاعات: 14
```
مع زرين: "👤 معلومات المالك" (يوديك مباشرة لصفحة صاحب هذا البوت) و"🗑️ حذف البوت" (يسألك تأكيد، وبعد التأكيد يوقف الـ poller ويمسح البوت نهائياً).

### 👤 أصحاب البوتات
نفس فكرة البحث والقائمة، بس على مستوى "المالك" مو "البوت". تفتح مالك تشوف: كل بوتاته (أزرار تفتح كل وحدة)، إجمالي مستخدمي كل بوتاته، وزرين: "🚫 حظر من المصنع" (يوديك مباشرة لشاشة الحظر بهذا الـ ID جاهز) و"📨 إرسال رسالة" (ترسل نص، يوصله مباشرة كرسالة من بوت المصنع).

### 📊 الإحصائيات
4 أزرار فترات (اليوم / 7 أيام / 30 يوم / الكل) — كل وحدة تعرض نفس الأرقام الحقيقية بس بعنوان الفترة (ملاحظة صريحة: الأرقام حالياً تراكمية من قاعدة البيانات، مو مقسّمة فعلياً حسب الفترة الزمنية المحددة إلا لعدد "رسائل اليوم/إذاعات اليوم" اللي هي فعلاً يومية).

### 📢 الإعلانات
1. تختار الجمهور: **جميع المستخدمين** / **أصحاب البوتات فقط** / **الجدد اليوم** / **النشطون (7 أيام)** / **غير النشطين (30 يوم)**
2. ترسل نص الإعلان
3. يطلعلك تأكيد: "الجمهور: X مستخدم" + نص الرسالة
4. تضغط "✅ إرسال" → يرسلها فعلياً لكل واحد بالقائمة، وحدة وحدة، بالخلفية

**الجمهور محسوب من مين راسل بوت المصنع فعلياً** (جدول منفصل `factory_users`، مو مستخدمي البوتات المصنوعة).

### 🚫 الحظر
"➕ حظر مستخدم" → ترسل الـ ID → يطلعلك 3 مفاتيح تبديل (تقدر تفعّل أكثر من وحدة سوا):

| المفتاح | شنو يسوي فعلياً |
|---|---|
| ☑ من استخدام المصنع | ما يقدر يستخدم بوت المصنع نفسه إطلاقاً بعدها |
| ☑ من إنشاء بوتات جديدة | يقدر يستخدم المصنع، بس أي توكن يرسله يترفض |
| ☑ إيقاف بوتاته الحالية | **يوقف الـ poller فوراً** لكل بوتاته الموجودة (توقف عن استقبال رسائل، بدون حذفها) |

بعد ما تحدد، تضغط "🚫 تأكيد الحظر". فيه زر "♻️ إلغاء الحظر كلياً" يمسح كل القيود مرة وحدة.

### 🔐 الاشتراك الإجباري (مستوى المصنع)
تضيف قنوات هنا، وتنطبق تلقائياً على **بوت المصنع نفسه + كل بوت مصنوع** (فوق أي قناة يضيفها صاحب بوت معين من لوحته الخاصة — تصير الاثنتين مطلوبتين سوا).

### 💾 النسخ الاحتياطي (حصري هنا فقط)
"💾 إنشاء نسخة الآن" → يسحب **قاعدة البيانات كاملة** (كل البوتات، كل التوكنات، كل المستخدمين) بملف `.zip` فيه CSV لكل جدول. "📦 النسخ السابقة" تعرض كل نسخة سابقة، وتضغط عليها يطلعلك زر "♻️ استعادة" (يسألك تأكيد قبل، ويضيف بيانات النسخة فوق الموجود حالياً — البيانات المكررة تتجاهل تلقائياً).

### 🗄️ قاعدة البيانات
يعرض حجم الملف + عدد البوتات/المستخدمين/الرسائل. زر "🧹 تنظيف البيانات القديمة" يمسح الرسائل والسجلات القديمة (حسب مدة الاحتفاظ المحددة بـ`.env`).

### ⚙️ إعدادات المصنع
- 🤖 حد البوتات لكل مستخدم — رقم تحدده، لو وصله يترفض إنشاء بوت جديد
- 📢 حد المستخدمين بالإذاعة — سقف أقصى لعدد المستهدفين بإذاعة وحدة

### 🛠️ النظام والصيانة
يعرض حالة حية: عدد الـ Pollers الشغالة، عدد المهام بالطابور. زر "🔄 إعادة تحميل البوتات" يعيد ربط كل بوت مسجل بمهمة polling (مفيد لو صار خلل بوقت التشغيل). زر "🧹 تنظيف السجلات القديمة".

### 📋 السجلات
3 أزرار (أخطاء / تحذيرات / الكل) — تعرض آخر 15 حدث مسجل فعلياً بقاعدة البيانات (وقت الخطأ، مصدره، نصه). **مو كل شي بالنظام يتسجل هنا** — بس النقاط المهمة (فشل poller متكرر، انهيار خدمة أساسية) — باقي الأحداث العادية تطبع بس على الترمنال.

### 👑 المشرفون
"➕ إضافة مشرف" → ترسل الـ ID → يطلعلك 3 خيارات صلاحية:

| الدور | الصلاحيات |
|---|---|
| 👑 Admin | البوتات، المالكين، الإعلانات، السجلات |
| 🛠️ Support | البوتات، المالكين بس |
| 📊 Analyst | السجلات بس (قراءة) |

(أنت "Owner" دايماً — كل الصلاحيات، ما يظهر بهذي القائمة لأنك مو محتاج تضيف نفسك).

---

## 3️⃣ لوحة تحكم كل بوت مصنوع — لصاحبه بس

يدخلها صاحب البوت (الـ owner_id المسجل وقت الإنشاء) + أي "مسؤول" يضيفه من داخل لوحته. **ما عندها أي وصول لمعلومات المصنع أو البوتات الثانية** — معزولة كلياً.

### الشاشة الرئيسية
إحصائيات اليوم (حقيقية لهذا البوت بس) + 6 أقسام: ⚙️ الإعدادات، 📝 المحتوى، 👥 المستخدمون، 🔐 الاشتراك، 🚚 التواصل، 🌐 النظام، ❓ الدليل.

### ⚙️ الإعدادات
| القسم | شنو يسوي فعلياً |
|---|---|
| 🛂 التحقق من العضوية | يفعّل بوابة تحقق حقيقية قبل أي مستخدم جديد يستخدم البوت — 5 طرق (تفصيل بالقسم 6) |
| 🔒 حماية المحتوى | يفعّل خاصية تيليجرام الرسمية (`protect_content`) تمنع فعلياً توجيه/حفظ رسائل البوت |
| 🔔 الإشعارات | تفعيل/تعطيل إشعار حقيقي يوصلك لما يدخل مستخدم جديد أو يحظر البوت |
| ⏱️ الحذف التلقائي | يحذف رسائل البوت (وممكن رسائل المستخدم) تلقائياً بعد مدة، بخلفية حقيقية تشتغل كل 15 ثانية |
| 🔔 تذكير غير النشطين | يرسل تذكير تلقائي لمين توقف عن التفاعل، بمراحل متعددة قابلة للإضافة (تذكير بعد X يوم، وثاني بعد Y يوم) |
| ⚡ ردود سريعة | تخزن ردود جاهزة تستخدمها بالرد على رسائل المستخدمين المحوّلة (`/r1 /r2`...) |

### 📝 المحتوى
👋 رسالة الترحيب (نص مخصص حقيقي)، 💬 الردود التلقائية (كلمة مفتاحية regex + رد، حتى مع أزرار روابط اختيارية)، 🎚 الأزرار الشفافة (أزرار ثابتة تظهر تحت رسالة الترحيب — الاستثناء الوحيد لقاعدة "بدون أزرار تحت"، لأنها للمستخدم العادي مو للوحة نفسها)، ✂️ الاختصارات (تُنشر فعلياً كقائمة أوامر `/` بتيليجرام)، ✏️ تعديل المحتوى (تخصيص نصوص جاهزة زي رسالة `/help`)، ℹ️ معلومات البوت.

### 👥 المستخدمون
إحصائيات، إدارة مسؤولين إضافيين، حظر/فك حظر يدوي، سجل نشاط.

### 🔐 الاشتراك الإجباري (خاص بهذا البوت)
حتى 10 قنوات (حد مطبّق فعلياً)، تفعيل/تعطيل فردي وجماعي، معاينة شكل البوابة قبل النشر، ونص زر التحقق قابل للتخصيص.

### 🚚 التواصل
📣 الإذاعة (نص → تأكيد → إرسال فعلي بدفعات، بدون استهلاك رام، مع نقطة استئناف لو انقطع)، ⚡ إذاعة سريعة (نفس الشي بس بدون خطوة تأكيد)، وتفعيل/تعطيل 🔄 توجيه الرسائل.

### 🌐 النظام
🔄 آخر التحديثات (ثابت، معلوماتي). **لا يوجد نسخ احتياطي هنا** — انحذف نهائياً لأسباب أمنية (كان يكشف بيانات بوتات ثانية).

---

## 4️⃣ تجربة المستخدم العادي (يراسل بوت مصنوع، مو الأدمن)

- `/start` → يستلم بس رسالة ترحيب (المخصصة لو الأدمن غيّرها، والأزرار الشفافة إن وجدت)
- لو فيه اشتراك إجباري (بوت + مصنع سوا) → لازم يشترك أول
- لو فيه تحقق مفعّل → يمر بتحدي (CAPTCHA/رقم/رابط/موافقة يدوية) قبل أي شي
- كلمات معينة تطلق ردود تلقائية جاهزة
- لو "التوجيه" مفعّل، رسالته توصل صاحب البوت مباشرة، ورده يرجعله تلقائي
- صفر أزرار إدارة — ما يشوف أي شي متعلق باللوحة

---

## 5️⃣ الآليات الخلفية (Mechanisms)

### ⏱️ Scheduler (`scheduler.py`)
يشتغل بالخلفية بشكل دائم، فيه حلقتين:
- **كل 15 ثانية**: يفحص جدول "رسائل مجدولة للحذف"، ويحذفها فعلياً من تيليجرام لما يوصل وقتها
- **كل 5 دقائق** (أو حسب ما يحدده كل بوت): يفحص المستخدمين الخاملين، يرسلهم تذكير حسب المراحل المحددة، ويكتشف رجوعهم تلقائياً

### 🛂 التحقق (`verification.py`)
5 طرق حقيقية: دخول مباشر (زر وحد)، CAPTCHA (سؤال حسابي)، زيارة موقع (تأكيد ذاتي — بصراحة ما نقدر نتحقق تقنياً أنه زار فعلاً)، مشاركة رقم الهاتف (تحقق حقيقي عبر تيليجرام)، قبول يدوي (يوصلك طلب وتوافق بنفسك).

### 🔄 التوجيه (`forwarding.py`)
لما مستخدم يرسل رسالة، تنحول لمحادثتك مع البوت مباشرة (`forwardMessage` الرسمية). ترد عليها عادي، أو تستخدم `/r1` `/r2` (ردودك السريعة المحفوظة) وردك يوصله تلقائياً.

### 🔐 بوابة الاشتراك (`subscription_handler.py`)
تجمع كل القنوات الناقصة (بوت + مصنع) برسالة وحدة، كل قناة تحصل رابط انضمام حقيقي (حتى القنوات الخاصة -100 عبر `createChatInviteLink`)، وزر "✅ تحقق" يعيد الفحص فوراً.

---

## 6️⃣ التشغيل الفعلي (كيف يشتغل من الداخل)

```
main.py يشغّل 4 مهام متوازية بنفس الوقت:
├─ Poller Supervisor   → مهمة polling مستقلة لكل بوت (المصنع + كل بوت مصنوع)
├─ Priority Queue      → معالجة الرسائل بأولويات (4 workers)
├─ Health Monitor      → يطبع حالة النظام كل 60 ثانية
└─ Scheduler           → الحذف التلقائي + التذكيرات
```

- **قاعدة بيانات وحدة** (SQLite) تخدم كل شي، كل بوت معزول بياناتياً عن غيره بـ `bot_id`
- **صفر مكتبات pip خارجية** — بس `sqlite3` و`urllib` المدمجات ببايثون (`asqlite.py` و`telegram_adapter.py` بديلين مبنيين بالكامل)
- أي بوت جديد يتسجل من بوت المصنع يبدأ `polling` حي **بنفس اللحظة**، بدون إعادة تشغيل `main.py`

### التشغيل على تيرمكس
```bash
cd ~/factory
bash install.sh        # pkg install python sqlite - بس
# ضيف FACTORY_BOT_TOKEN بملف .env
python main.py
```

---

## 7️⃣ ملخص الملفات (54 ملف)

| المجلد/الملف | المسؤولية |
|---|---|
| `factory_bot.py` | بوت المصنع (تجربة أي شخص عادي) |
| `factory_admin/` | لوحة تحكم المصنع (7 ملفات) |
| `admin_panel/` | لوحة تحكم كل بوت مصنوع (8 ملفات) |
| `scheduler.py` | الحذف التلقائي + التذكيرات |
| `verification.py` | نظام التحقق |
| `forwarding.py` | توجيه الرسائل |
| `subscription_handler.py` | الاشتراك الإجباري (بوت + مصنع) |
| `db.py` / `asqlite.py` | قاعدة البيانات |
| `poller.py` / `dispatcher.py` / `telegram_adapter.py` | استقبال/توزيع/إرسال |
| `message_handler.py` / `message_sender.py` | معالجة رسائل المستخدم العادي |
| `priority_queue.py` / `rate_limiter.py` / `broadcast_engine.py` | الأداء والإذاعة |
| `backup.py` | النسخ الاحتياطي (حصري بلوحة المصنع) |
| `main.py` | نقطة التشغيل |

---

## ⚠️ صراحة — اللي لسا محدوديات معروفة

- التوجيه يوصل لصاحب البوت الأساسي بس، مو لكل المسؤولين الإضافيين
- القوائم الطويلة (أكثر من ~15 عنصر) ما فيها صفحات بكل مكان — بس بلوحة المصنع (البوتات/المالكين) فيها ترقيم صفحات فعلي
- "حماية الميديا" غير فعّالة لأن المحرك كامل نص بس، ما يرسل صور/فيديو
- السجلات (📋) تسجل النقاط المهمة بس، مو كل حدث بالنظام
